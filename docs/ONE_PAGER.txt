╔══════════════════════════════════════════════════════════════════════════════╗
║                          GoDNS CLI Quick Reference                           ║
║                              One-Page Printable                              ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌─ MOST COMMON COMMANDS ────────────────────────────────────────────────────────┐
│                                                                               │
│  ./bin/godnscli d                # Discover server & find domains            │
│  ./bin/godnscli t                # Run all tests                             │
│  ./bin/godnscli q example.lan    # Query a domain (A record)                 │
│  ./bin/godnscli h                # Check server health                       │
│  ./bin/godnscli v                # Show version                              │
│                                                                               │
└───────────────────────────────────────────────────────────────────────────────┘

┌─ QUERY DIFFERENT RECORD TYPES ────────────────────────────────────────────────┐
│                                                                               │
│  ./bin/godnscli q example.lan -t A       # IPv4 address                      │
│  ./bin/godnscli q example.lan -t AAAA    # IPv6 address                      │
│  ./bin/godnscli q example.lan -t MX      # Mail servers                      │
│  ./bin/godnscli q example.lan -t NS      # Name servers                      │
│  ./bin/godnscli q example.lan -t TXT     # Text records                      │
│                                                                               │
└───────────────────────────────────────────────────────────────────────────────┘

┌─ QUERY DIFFERENT SERVERS ─────────────────────────────────────────────────────┐
│                                                                               │
│  ./bin/godnscli q example.lan -s localhost:53        # Local server          │
│  ./bin/godnscli q example.lan -s 192.168.1.100:53   # Custom server          │
│  ./bin/godnscli q google.com -s 8.8.8.8:53          # Google DNS             │
│                                                                               │
└───────────────────────────────────────────────────────────────────────────────┘

┌─ VERBOSE OUTPUT ──────────────────────────────────────────────────────────────┐
│                                                                               │
│  ./bin/godnscli q example.lan -v    # Detailed query output                  │
│  ./bin/godnscli h -v                # Detailed health check                  │
│  ./bin/godnscli t -v                # Detailed test output                   │
│                                                                               │
└───────────────────────────────────────────────────────────────────────────────┘

┌─ GLOBAL FLAGS ────────────────────────────────────────────────────────────────┐
│                                                                               │
│  -s, --server     DNS server address (default: localhost:53)                 │
│  -v, --verbose    Show detailed output                                       │
│  -h, --help       Show help                                                  │
│                                                                               │
└───────────────────────────────────────────────────────────────────────────────┘

┌─ QUERY FLAGS ─────────────────────────────────────────────────────────────────┐
│                                                                               │
│  -t, --type       Record type (A, AAAA, MX, NS, TXT, CNAME, SOA, PTR, SRV)  │
│  --timeout        Timeout in seconds (default: 5)                            │
│                                                                               │
└───────────────────────────────────────────────────────────────────────────────┘

┌─ HEALTH FLAGS ────────────────────────────────────────────────────────────────┐
│                                                                               │
│  --liveness-port     Liveness probe port (default: 14080)                     │
│  --readiness-port    Readiness probe port (default: 14081)                    │
│                                                                               │
└───────────────────────────────────────────────────────────────────────────────┘

┌─ BUILD COMMANDS ──────────────────────────────────────────────────────────────┐
│                                                                               │
│  make build-cli    # Build CLI tool                                          │
│  make build-all    # Build everything (server + CLI)                         │
│  make lint         # Run linters                                             │
│                                                                               │
└───────────────────────────────────────────────────────────────────────────────┘

┌─ DOCKER COMMANDS ─────────────────────────────────────────────────────────────┐
│                                                                               │
│  docker-compose up -d          # Start server                                │
│  docker-compose down           # Stop server                                 │
│  docker-compose logs godns     # View logs                                   │
│  docker-compose restart        # Restart server                              │
│                                                                               │
└───────────────────────────────────────────────────────────────────────────────┘

┌─ QUICK SETUP ─────────────────────────────────────────────────────────────────┐
│                                                                               │
│  1. make build-cli              # Build the CLI                              │
│  2. docker-compose up -d        # Start server                               │
│  3. ./hack/add-test-zone.sh test.lan 192.168.1.100  # Add test zone         │
│  4. ./bin/godnscli d            # Discover what's available                  │
│  5. ./bin/godnscli t            # Run tests                                  │
│  6. ./bin/godnscli q test.lan   # Query domain                               │
│                                                                               │
└───────────────────────────────────────────────────────────────────────────────┘

┌─ FINDING DOMAINS TO QUERY ────────────────────────────────────────────────────┐
│                                                                               │
│  # Discover server and get help finding domains                              │
│  ./bin/godnscli d -v                                                         │
│                                                                               │
│  # List all zones in Valkey                                                  │
│  docker-compose exec valkey valkey-cli -a mysecretpassword KEYS 'dns:zone:*' │
│                                                                               │
│  # Add a test zone                                                           │
│  ./hack/add-test-zone.sh myapp.lan 192.168.1.50                             │
│                                                                               │
└───────────────────────────────────────────────────────────────────────────────┘

┌─ TROUBLESHOOTING ─────────────────────────────────────────────────────────────┐
│                                                                               │
│  Problem: Don't know what to query                                           │
│  Fix:     ./bin/godnscli d -v                                                │
│                                                                               │
│  Problem: Server not responding                                              │
│  Fix:     ./bin/godnscli h -v                                                │
│                                                                               │
│  Problem: Query timeout                                                      │
│  Fix:     ./bin/godnscli q example.lan --timeout 30                          │
│                                                                               │
│  Problem: No answer section                                                  │
│  Fix:     ./bin/godnscli q example.lan -v                                    │
│                                                                               │
└───────────────────────────────────────────────────────────────────────────────┘

┌─ DOCUMENTATION ───────────────────────────────────────────────────────────────┐
│                                                                               │
│  docs/CLI_CHEAT_SHEET.md         - This file                                 │
│  docs/CLI_QUICK_REFERENCE.md     - Detailed reference                        │
│  docs/CLI_GUIDE.md               - Complete guide                            │
│  docs/QUICK_START.md             - 5-minute setup                            │
│                                                                               │
└───────────────────────────────────────────────────────────────────────────────┘

┌─ GETTING HELP ────────────────────────────────────────────────────────────────┐
│                                                                               │
│  ./bin/godnscli --help           # General help                              │
│  ./bin/godnscli query --help     # Query command help                        │
│  ./bin/godnscli health --help    # Health command help                       │
│  ./bin/godnscli test --help      # Test command help                         │
│                                                                               │
└───────────────────────────────────────────────────────────────────────────────┘

╔══════════════════════════════════════════════════════════════════════════════╗
║                     📌 Keep this handy while working! 📌                     ║
╚══════════════════════════════════════════════════════════════════════════════╝
