FROM alpine:latest

# Install dependencies
RUN apk add --no-cache curl jq bash

# Copy initialization script
COPY init-keycloak.sh /init-keycloak.sh
RUN chmod +x /init-keycloak.sh

ENTRYPOINT ["/init-keycloak.sh"]
