apiVersion: v1
kind: Service
metadata:
  name: {{ include "godns.fullname" . }}
  labels:
    {{- include "godns.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "godns.selectorLabels" . | nindent 4 }}
